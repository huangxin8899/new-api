# 复制为 /root/new-api/.env 后修改
# cp .env.example .env

POSTGRES_USER=root
POSTGRES_PASSWORD=请改成强密码
POSTGRES_DB=new-api

# 多机部署必须改；单机也建议固定为随机长字符串，升级时不要变
SESSION_SECRET=请改成随机长字符串

# 审计日志节点名（可选）
NODE_NAME=new-api-node-1

# update.sh 使用的 git 分支（与 fork 上的二开分支一致）
DEPLOY_BRANCH=custom
